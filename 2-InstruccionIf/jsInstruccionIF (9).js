function mostrar()
{
	//Genero el número RANDOM entre 1 y 10 
   
    var random;
    var random =Math.random(random) * 10; 
    var random =Math.round(random);

   if (random)
   {
   	alert(random);
   }
   
}//FIN DE LA FUNCIÓN